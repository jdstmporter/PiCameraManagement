{
  "name": "mediamtx",
  "version": "0.0.1",
  "description": "manage picam via mediamtx",
  "main": "",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
